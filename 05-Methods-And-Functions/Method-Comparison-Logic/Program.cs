// See https://aka.ms/new-console-template for more information






// Local method comparison example

int largerNumber = CompareNumbers(3,5);
Console.WriteLine(largerNumber);
Console.ReadKey();

// Returns the greater of the two integers
int CompareNumbers(int a, int b)
{
    return (a > b) ? a : b;
}
